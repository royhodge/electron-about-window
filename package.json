{
  "name": "electron-multichain",
  "version": "1.10.0",
  "description": "'About App' window for Electron application",
  "main": "src/index.js",
  "scripts": {
    "dep": "npm install",
    "build": "tsc --pretty -p src/",
    "watch": "tsc --watch --pretty -p src/",
    "tslint": "tslint -p ./src",
    "stylelint": "stylelint styles/*.css",
    "lint": "npm run tslint && npm run stylelint",
    "example": "NODE_ENV=production electron ./example",
    "debug": "electron ./example",
    "start": "npm run dep && npm run build && npm run example",
    "preversion": "npm run lint && npm run build"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/royhodge/electron-multichain.git"
  },
  "keywords": [
    "Electron",
    "electron-component",
    "about",
    "window"
  ],
  "author": "rh <roy.hodge@mail.com>",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/royhodge/electron-multichain/issues"
  },
  "homepage": "https://github.com/royhodge/electron-multichain#readme",
  "devDependencies": {
    "@types/node": "^9.4.6",
    "electron": "^1.8.2",
    "stylelint": "^9.0.0",
    "stylelint-config-standard": "^18.1.0",
    "tslint": "^5.9.1",
    "typescript": "^2.7.2"
  },
  "dependencies": {}
}
